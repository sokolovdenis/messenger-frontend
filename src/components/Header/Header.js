import React from 'react';
import Title from '../Title/Title';
import './Header.css';

const Header = () => (
    <header className="Header">
        <Title/>
    </header>
);

export default Header;
