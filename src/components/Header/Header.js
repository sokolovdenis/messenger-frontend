import React from 'react';
import Title from '../Title/Title';
import './Header.css';

const Header = () => {
    return (
        <div className="Header">
            <Title />
        </div>
    );
};

export default Header;